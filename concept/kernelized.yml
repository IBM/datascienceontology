schema: concept
id: kernelized
kind: function
name: kernelized
description: >
  kernelized version of a statistical model based on the Euclidean inner
  product, via the "kernel trick"
inputs:
  - type: euclidean-model
outputs:
  - type: kernel-model
external: 
  wikidata: Q1739323
