schema: concept
id: predict
kind: function
name: predict
description: make predictions using a supervised model
inputs:
  - type: supervised-model
    name: model
  - type: data
    name: predictors
outputs:
  - type: data
    name: predicted
