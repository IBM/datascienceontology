schema: concept
id: design-matrix
name: design matrix
description: >
  design matrix, also known as model matrix, of a linear model or
  generalized linear model
kind: function
inputs:
  - type: glm
    name: model
outputs:
  - type: matrix
    name: design-matrix
