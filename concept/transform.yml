schema: concept
id: transform
kind: function
name: transform
description: transform data using a transformer
inputs:
  - type: transformer
  - type: data
outputs:
  - type: data
    name: transformed
