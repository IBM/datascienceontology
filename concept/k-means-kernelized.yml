schema: concept
id: k-means-kernelized
kind: function
name: kernelized
description: k-means clustering as a special case of kernel k-means clustering
is-a: kernelized
inputs:
  - type: k-means
outputs:
  - type: kernel-k-means
