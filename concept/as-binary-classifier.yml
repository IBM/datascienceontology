schema: concept
id: as-binary-classifier
name: use multiclass classifier for binary classification
description: binary classification as special case of multiclass classification with two classes
kind: function
inputs:
  - type: multiclass-classifier
outputs:
  - type: binary-classifier
